```
新增接口文档.md
# 新增接口文档

## 1. 社区信息管理模块

### 1.1 根据社区名称或详细地址分页查询社区信息

- **接口地址**: `/api/community/search`
- **请求方法**: `GET`
- **接口描述**: 根据社区名称或详细地址分页查询社区信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `communityName`: 社区名称（可选）
  - `detailAddress`: 详细地址（可选）
- **返回结果**: 社区信息分页数据

## 2. 楼栋信息管理模块

### 2.1 根据楼栋名称或楼栋编号分页查询楼栋信息

- **接口地址**: `/api/building/search`
- **请求方法**: `GET`
- **接口描述**: 根据楼栋名称或楼栋编号分页查询楼栋信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `buildingName`: 楼栋名称（可选）
  - `buildingNo`: 楼栋编号（可选）
- **返回结果**: 楼栋信息分页数据

## 3. 房屋信息管理模块

### 3.1 根据房屋编码或房号分页查询房屋信息

- **接口地址**: `/api/house/search`
- **请求方法**: `GET`
- **接口描述**: 根据房屋编码或房号分页查询房屋信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `houseCode`: 房屋编码（可选）
  - `roomNo`: 房号（可选）
- **返回结果**: 房屋信息分页数据

## 4. 停车场信息管理模块

### 4.1 根据停车场名称或停车场编码分页查询停车场信息

- **接口地址**: `/api/parkingLot/search`
- **请求方法**: `GET`
- **接口描述**: 根据停车场名称或停车场编码分页查询停车场信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `lotName`: 停车场名称（可选）
  - `lotCode`: 停车场编码（可选）
- **返回结果**: 停车场信息分页数据

## 5. 车位信息管理模块

### 5.1 根据车位编号或车位号分页查询车位信息

- **接口地址**: `/api/parkingSpace/search`
- **请求方法**: `GET`
- **接口描述**: 根据车位编号或车位号分页查询车位信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `spaceNo`: 车位编号或车位号（可选）
- **返回结果**: 车位信息分页数据

## 6. 车辆信息管理模块

### 6.1 根据车牌号或车主ID分页查询车辆信息

- **接口地址**: `/api/vehicle/search`
- **请求方法**: `GET`
- **接口描述**: 根据车牌号或车主ID分页查询车辆信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `plateNumber`: 车牌号（可选）
  - `ownerId`: 车主ID（可选）
- **返回结果**: 车辆信息分页数据

## 7. 门禁设备管理模块

### 7.1 根据设备名称或设备编码分页查询门禁设备信息

- **接口地址**: `/api/accessControlDevice/search`
- **请求方法**: `GET`
- **接口描述**: 根据设备名称或设备编码分页查询门禁设备信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `deviceName`: 设备名称（可选）
  - `deviceCode`: 设备编码（可选）
- **返回结果**: 门禁设备信息分页数据

## 8. 门禁记录管理模块

### 8.1 根据设备ID或人员姓名或通行方式或验证结果分页查询门禁记录信息

- **接口地址**: `/api/accessControlRecord/search`
- **请求方法**: `GET`
- **接口描述**: 根据设备ID或人员姓名或通行方式或验证结果分页查询门禁记录信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `deviceId`: 设备ID（可选）
  - `personName`: 人员姓名（可选）
  - `accessMethod`: 通行方式（可选）
  - `verifyResult`: 验证结果（可选）
- **返回结果**: 门禁记录信息分页数据

## 9. 仪表配置管理模块

### 9.1 根据分类名称或仪表类型或状态分页查询仪表配置信息

- **接口地址**: `/api/meterConfig/search`
- **请求方法**: `GET`
- **接口描述**: 根据分类名称或仪表类型或状态分页查询仪表配置信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `categoryName`: 分类名称（可选）
  - `meterType`: 仪表类型（可选）
  - `status`: 状态（可选）
- **返回结果**: 仪表配置信息分页数据

## 10. 仪表信息管理模块

### 10.1 根据仪表编码或仪表名称或位置类型或仪表状态分页查询仪表信息

- **接口地址**: `/api/meterInfo/search`
- **请求方法**: `GET`
- **接口描述**: 根据仪表编码或仪表名称或位置类型或仪表状态分页查询仪表信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `meterCode`: 仪表编码（可选）
  - `meterName`: 仪表名称（可选）
  - `locationType`: 位置类型（可选）
  - `meterStatus`: 仪表状态（可选）
- **返回结果**: 仪表信息分页数据

## 11. 抄表记录管理模块

### 11.1 根据仪表ID或抄表人或抄表类型或抄表状态分页查询抄表记录

- **接口地址**: `/api/meterReading/search`
- **请求方法**: `GET`
- **接口描述**: 根据仪表ID或抄表人或抄表类型或抄表状态分页查询抄表记录
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `meterId`: 仪表ID（可选）
  - `readerName`: 抄表人（可选）
  - `readingType`: 抄表类型（可选）
  - `readingStatus`: 抄表状态（可选）
- **返回结果**: 抄表记录分页数据

## 12. 员工信息管理模块

### 12.1 根据员工姓名或手机号或工号或工作状态分页查询员工信息

- **接口地址**: `/api/staff/search`
- **请求方法**: `GET`
- **接口描述**: 根据员工姓名或手机号或工号或工作状态分页查询员工信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `name`: 员工姓名（可选）
  - `phone`: 手机号（可选）
  - `workNo`: 工号（可选）
  - `workStatus`: 工作状态（可选）
- **返回结果**: 员工信息分页数据

## 13. 业主信息管理模块

### 13.1 根据业主姓名或手机号或业主类型或状态分页查询业主信息

- **接口地址**: `/api/owner/search`
- **请求方法**: `GET`
- **接口描述**: 根据业主姓名或手机号或业主类型或状态分页查询业主信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `name`: 业主姓名（可选）
  - `phone`: 手机号（可选）
  - `ownerType`: 业主类型（可选）
  - `status`: 状态（可选）
- **返回结果**: 业主信息分页数据

## 14. 部门信息管理模块

### 14.1 根据部门名称或部门编码或状态分页查询部门信息

- **接口地址**: `/api/department/search`
- **请求方法**: `GET`
- **接口描述**: 根据部门名称或部门编码或状态分页查询部门信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `departmentName`: 部门名称（可选）
  - `departmentCode`: 部门编码（可选）
  - `status`: 状态（可选）
- **返回结果**: 部门信息分页数据

## 15. 角色信息管理模块

### 15.1 根据角色名称或角色编码或角色类型或状态分页查询角色信息

- **接口地址**: `/api/role/search`
- **请求方法**: `GET`
- **接口描述**: 根据角色名称或角色编码或角色类型或状态分页查询角色信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `roleName`: 角色名称（可选）
  - `roleCode`: 角色编码（可选）
  - `roleType`: 角色类型（可选）
  - `status`: 状态（可选）
- **返回结果**: 角色信息分页数据

## 16. 知识库管理模块

### 16.1 根据标题分页查询知识库信息

- **接口地址**: `/api/smartQaKnowledge/search`
- **请求方法**: `GET`
- **接口描述**: 根据标题分页查询知识库信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `title`: 标题（可选）
- **返回结果**: 知识库信息分页数据

## 17. 社区公告管理模块

### 17.1 根据公告标题或公告类型或状态分页查询社区公告信息

- **接口地址**: `/api/communityNotice/search`
- **请求方法**: `GET`
- **接口描述**: 根据公告标题或公告类型或状态分页查询社区公告信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `title`: 公告标题（可选）
  - `noticeType`: 公告类型（可选）
  - `status`: 状态（可选）
- **返回结果**: 社区公告信息分页数据

## 18. 问题跟进管理模块

### 18.1 根据问题ID或跟进人分页查询问题跟进信息

- **接口地址**: `/api/issueFollowUp/search`
- **请求方法**: `GET`
- **接口描述**: 根据问题ID或跟进人分页查询问题跟进信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `issueId`: 问题ID（可选）
  - `operatorName`: 跟进人（可选）
- **返回结果**: 问题跟进信息分页数据

## 19. 业主问题管理模块

### 19.1 根据业主ID或问题类型或状态分页查询业主问题信息

- **接口地址**: `/api/ownerIssue/search`
- **请求方法**: `GET`
- **接口描述**: 根据业主ID或问题类型或状态分页查询业主问题信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `ownerId`: 业主ID（可选）
  - `issueType`: 问题类型（可选）
  - `issueStatus`: 状态（可选）
- **返回结果**: 业主问题信息分页数据

## 20. 系统管理员管理模块

### 20.1 根据用户名或真实姓名或状态分页查询系统管理员信息

- **接口地址**: `/api/systemAdmin/search`
- **请求方法**: `GET`
- **接口描述**: 根据用户名或真实姓名或状态分页查询系统管理员信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `username`: 用户名（可选）
  - `realName`: 真实姓名（可选）
  - `status`: 状态（可选）
- **返回结果**: 系统管理员信息分页数据

## 21. 管理员操作日志管理模块

### 21.1 根据管理员ID或操作类型或操作描述分页查询操作日志信息

- **接口地址**: `/api/adminOperationLog/search`
- **请求方法**: `GET`
- **接口描述**: 根据管理员ID或操作类型或操作描述分页查询操作日志信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `adminId`: 管理员ID（可选）
  - `operationType`: 操作类型（可选）
  - `operationDescription`: 操作描述（可选）
- **返回结果**: 管理员操作日志信息分页数据

## 22. 停车记录管理模块

### 22.1 根据车位ID或车牌号分页查询停车记录信息

- **接口地址**: `/api/parkingRecord/search`
- **请求方法**: `GET`
- **接口描述**: 根据车位ID或车牌号分页查询停车记录信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `parkingSpaceId`: 车位ID（可选）
  - `plateNumber`: 车牌号（可选）
- **返回结果**: 停车记录信息分页数据

## 23. 房屋业主关联管理模块

### 23.1 分页查询房屋业主关联详细信息

- **接口地址**: `/api/houseOwner/pageDetail`
- **请求方法**: `GET`
- **接口描述**: 分页查询房屋业主关联详细信息，包含房屋和业主信息
- **请求参数**:
  - `pageNum`: 当前页码（可选，默认为1）
  - `pageSize`: 每页大小（可选，默认为10）
  - `houseId`: 房屋ID（可选）
  - `ownerId`: 业主ID（可选）
  - `relationship`: 关系类型（可选）
  - `status`: 状态（可选）
- **返回结果**: 房屋业主关联详细信息分页数据

### 23.2 根据ID查询房屋业主关联详细信息

- **接口地址**: `/api/houseOwner/detail/{id}`
- **请求方法**: `GET`
- **接口描述**: 根据ID查询房屋业主关联详细信息，包含房屋和业主信息
- **请求参数**:
  - `id`: 房屋业主关联ID（必填）
- **返回结果**: 房屋业主关联详细信息

## 24. 复杂业务管理模块

### 24.1 根据业主ID查询车位和车辆信息

- **接口地址**: `/api/complex/owner/{ownerId}/parkingInfo`
- **请求方法**: `GET`
- **接口描述**: 根据业主ID查询车位和车辆信息
- **请求参数**:
  - `ownerId`: 业主ID（必填）
- **返回结果**: 业主车位和车辆信息

### 24.2 根据车位ID查询车位和停车场详细信息

- **接口地址**: `/api/complex/parkingSpace/{parkingSpaceId}/detail`
- **请求方法**: `GET`
- **接口描述**: 根据车位ID查询车位和停车场详细信息
- **请求参数**:
  - `parkingSpaceId`: 车位ID（必填）
- **返回结果**: 车位和停车场详细信息

### 24.3 根据停车场ID查询停车场和社区详细信息

- **接口地址**: `/api/complex/parkingLot/{parkingLotId}/detail`
- **请求方法**: `GET`
- **接口描述**: 根据停车场ID查询停车场和社区详细信息
- **请求参数**:
  - `parkingLotId`: 停车场ID（必填）
- **返回结果**: 停车场和社区详细信息
```